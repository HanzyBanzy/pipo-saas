[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = [
  "pnpm --filter @pipo/types build",
  "pnpm --filter @pipo/db build",
  "pnpm --filter api build"
]

[start]
cmd = "node apps/api/dist/index.js"
